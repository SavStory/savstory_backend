import express from "express"

const route = express.Router()

route.post("/login", () => {
    // TODO - get login from page
})

route.get("/:user_id", () => {
    // TODO - make use of the user_id
})

